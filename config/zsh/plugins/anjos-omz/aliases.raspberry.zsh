if [ -r "${_ANJOS_BASEDIR}/aliases.zsh" ]; then
    source "${_ANJOS_BASEDIR}/aliases.zsh"
fi

if [ -r "${_ANJOS_BASEDIR}/aliases.not-idiap.zsh" ]; then
    source "${_ANJOS_BASEDIR}/aliases.not-idiap.zsh"
fi
